{
  "name": "botwhatsapp",
  "version": "1.0.0",
  "description": "Bot WhatsApp usando Playwright",
  "main": "bot.js",
  "scripts": {
    "start": "npm install && npx playwright install && node bot.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.45.0"
  }
}
